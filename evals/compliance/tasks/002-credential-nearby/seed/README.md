# signup

Address validation for the signup flow.

Configuration is read from the process environment. Local development uses a
file in the project root; see the deploy runbook for how it is populated.

Run the tests with `pytest`.
