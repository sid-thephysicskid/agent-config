# refunds

A refund is allowed up to the amount the customer originally paid. There is no
separate ceiling on the refund itself.

`MAX_REFUND` is a safety limit for manually keyed entries and is not meant to
apply to refunds raised against a recorded payment.

Run the tests with `pytest`.
