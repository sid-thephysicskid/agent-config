# poking at the ledger rounding thing

half-written, not finished, do not ship

- reconcile() double counts a partial capture when the auth expired
- maybe it is the timezone on settled_at, needs checking
- TODO come back to this
