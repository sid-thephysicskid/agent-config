# Contributing

[AGENTS.md](AGENTS.md) is the contributor guide, for people and agents alike.

The most useful contribution is a real command the guard refused while you were doing ordinary work. Add it to `hooks/ordinary.txt`, run `python3 hooks/tests.py --no-perf`, and open a PR with the failing case, or an issue if you'd rather not fix it.
