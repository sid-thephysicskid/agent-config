{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "if test -n \"${CLAUDE_PLUGIN_ROOT:-}\" && test -f \"$CLAUDE_PLUGIN_ROOT/hooks/guard-codex.py\"; then exec python3 \"$CLAUDE_PLUGIN_ROOT/hooks/guard-codex.py\"; fi; exit 0",
            "timeout": 5,
            "statusMessage": "Checking guardrails..."
          }
        ]
      }
    ]
  }
}
